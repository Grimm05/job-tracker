# .env  (à la racine, jamais committé — ajoute au .gitignore)

# PostgreSQL
POSTGRES_DB=jobtracker
POSTGRES_USER=jobtracker
POSTGRES_PASSWORD=change_me_in_prod

# Backend
DB_USERNAME=jobtracker
DB_PASSWORD=change_me_in_prod
JWT_SECRET=remplace_par_openssl_rand_hex_64_en_prod
JWT_EXPIRATION=86400000

# Ports exposés sur l'hôte
BACKEND_PORT=8080
FRONTEND_PORT=4200